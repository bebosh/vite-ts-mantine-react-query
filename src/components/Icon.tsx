import { ActionIcon } from '@mantine/core';
import { IconAdjustments } from '@tabler/icons';

export function Icon() {
  return (    
    <ActionIcon  color="violet" title="Warning">
      <IconAdjustments />
  </ActionIcon>
  );
}